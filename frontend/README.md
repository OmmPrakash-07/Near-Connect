# Near Connect frontend

The frontend is a React 19 + Vite application. Full project setup, demo accounts,
architecture, and API documentation are in the root [`README.md`](../README.md).

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` only when the API runs somewhere other than the
default `http://localhost:8080/api` address.
